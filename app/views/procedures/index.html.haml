%header

-# MAIN

%main
  = render :partial => 'layouts/top_navbar'

  %br
  %h3.container{:style => "margin: 0 auto;"} Procedimientos
  %br

  .container{:style => "margin: 0 auto;"}
    %a{:href => new_procedure_path, :class => "waves-effect waves-light btn blue"}
      %i.material-icons.right add
      Nuevo Procedimiento

  %br

  .row.container.card{:style => "margin: 0 auto;"}
    %table
      %thead
        %tr
          %th Descripción
          %th Precio
          %th
          %th
          %th

      %tbody
        - @procedures.each do |procedure|
          %tr
            %td= procedure.name
            %td= "$ #{number_to_currency(procedure.price, :format => "%u%n", :unit => '',:delimiter => ',',:separator => '.')}"
            -# %td= link_to 'Show', procedure
            %td= link_to 'Editar', edit_procedure_path(procedure)
            %td= link_to 'Eliminar', procedure, :method => :delete, :data => { :confirm => 'Esta Seguro?' }

    %br

-# = link_to 'New Procedure', new_procedure_path


= render :partial => 'layouts/footer'
-# END OF FOOTER
