%li.collection-item
  %i.material-icons.prefix perm_identity
  = current_user.email
  Ha agregado una orden nueva -
  - if activity.trackable
    %b Detalle:
    = link_to activity.trackable.id, activity.trackable
    %b Fecha:
    = activity.created_at.strftime("%d/%m/%Y - %I:%M%p")
  - else
    Ha Eliminado una orden

%br
